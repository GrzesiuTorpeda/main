name: MySQL
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
jobs:
  MySQL-Setup-Download:
    runs-on: self-hosted
    
    steps:
      - uses: actions/checkout@v3

      
      - name: MariaDB-Download
        run: echo "${{ secrets.USERPWD }}" | sudo  apt install mariadb-server -y
        
  Database-Create:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3

      - name: Database-Creation
        run: |
             echo "${{ secrets.MYSQL_ROOT_PASSWORD }}" | sudo mysql -u root -h localhost -p -e "create database a; use a; create table amongos (kolumna1 varchar(255), kolumna2 varchar(255), kolumna3 varchar(255));"
             sudo mysql -u root -h localhost -p -e "use a; insert into amongos (kolumna1,kolumna2,kolumna3) values ('wartosc1','wartosc2','wartosc3'); select * from amongos;"
